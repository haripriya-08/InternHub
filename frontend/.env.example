# Optional: use full backend URL if dev proxy doesn't work (e.g. when backend is on another port)
# VITE_API_URL=http://localhost:5000

# Production: set this in Vercel Environment Variables to your deployed backend URL
# VITE_API_URL=https://your-backend.railway.app
